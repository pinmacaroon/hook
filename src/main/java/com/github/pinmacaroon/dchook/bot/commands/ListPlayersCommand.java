package com.github.pinmacaroon.dchook.bot.commands;

import discord4j.core.event.domain.interaction.ChatInputInteractionEvent;
import discord4j.core.spec.InteractionApplicationCommandCallbackReplyMono;

public class ListPlayersCommand {
    public static InteractionApplicationCommandCallbackReplyMono handleCommand(ChatInputInteractionEvent event){
        //please kindly go fuck yourself
        /*
        var scs =
        PlayerManager playerManager = .getServer().getPlayerManager();
        List<ServerPlayerEntity> list = playerManager.getPlayerList();
        return event.reply();
         */
        return null;
    }
}
